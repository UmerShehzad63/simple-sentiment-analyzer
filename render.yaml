services:
  - type: web
    name: simple-sentiment-analyzer
    env: python
    buildCommand: "pip install -r requirements.txt && pip install -e ."
    startCommand: "gunicorn gui_server:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
